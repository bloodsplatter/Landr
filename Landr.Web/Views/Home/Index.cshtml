@model IReadOnlyCollection<IApp>
@{
    ViewData["Title"] = "Welcome";
}

@if (Model.Count < 1)
{
    @await Html.PartialAsync("_EmptyAppList")
}
else
{
    @await Html.PartialAsync("_AppList", Model)
}